# Database

This folder contains database assets for the distributed ticketing platform. Each service owns its own Supabase database and migration folder, so migrations are grouped under `auth-db`, `events-db`, `booking-db`, `payment-db`, and `ticket-db`.
