# Coordinator Database

The coordinator database stores the replicated coordinator cluster state for the distributed consensus and coordination demo.

Run migrations in order from `database/coordinator-db/migrations/` against the database referenced by `COORDINATOR_DATABASE_URL`.
