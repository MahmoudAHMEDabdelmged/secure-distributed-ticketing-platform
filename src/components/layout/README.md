# Layout Components

This folder contains shared layout components such as navigation, headers, footers, shells, and page wrappers. Keep app-wide layout pieces here instead of duplicating them across routes.
