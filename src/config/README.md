# Config

This folder contains centralized application configuration helpers and constants. Use it to normalize environment variables and avoid scattering configuration logic throughout the codebase.
