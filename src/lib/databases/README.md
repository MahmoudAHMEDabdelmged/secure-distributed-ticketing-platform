# Databases

This folder contains server-side database integration code for the Neon PostgreSQL service databases. The frontend must never connect directly to Neon; database access belongs in Next.js API routes and server-only modules.
