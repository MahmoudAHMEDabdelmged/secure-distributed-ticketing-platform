# Services

This folder contains standalone backend microservices for the Secure Distributed Ticketing Platform.

Current services:

- auth-service: handles registration, login, JWT issuing, and user identity.
- api-gateway: acts as the single entry point for backend services and applies security controls.
